---

interface Props {
	src: string;
	alt?: string;
}

const { src, alt = "" } = Astro.props;

---

<div class="carousel-slide">
	<div class="carousel-slide-content">
		<div class="carousel-slide-img"><img src={src} alt={alt}></div>
		<div class="carousel-slide-info"><slot /></div>
	</div>
</div>
